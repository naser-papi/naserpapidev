import { Meta, StoryObj } from "@storybook/react";
import { Works } from "@/components/template/landing";

const meta: Meta<typeof Works> = {
    component: Works,
    title: "template/landing/Works",
};
export default meta;

export const Default: StoryObj<typeof Works> = {};
