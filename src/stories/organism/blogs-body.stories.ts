import { Meta, StoryObj } from "@storybook/react";
import { BlogsBody } from "@/components";

const meta: Meta<typeof BlogsBody> = {
    component: BlogsBody,
    title: "organism/BlogsBody",
};
export default meta;

export const Default: StoryObj<typeof BlogsBody> = {};
