import { Meta, StoryObj } from "@storybook/react";
import { WorksHeader } from "@/components";

const meta: Meta<typeof WorksHeader> = {
    component: WorksHeader,
    title: "organism/WorksHeader",
};
export default meta;

export const Default: StoryObj<typeof WorksHeader> = {};
