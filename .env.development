# App
NODE_ENV=production
PORT=3000

# Feature flags (if used in your app)
SHOW_CONSTRUCTION=false
E2E_TEST=false

# --- Databases (inside Docker on 'backend' network) ---
# Postgres (Prisma example):
DATABASE_URL=postgresql://appuser:CHANGE_ME@postgres:5432/appdb?schema=public
# Mongo:
MONGO_URL=mongodb://root:CHANGE_ME@mongo:27017/yourdb?authSource=admin&replicaSet=rs0

# If your app calls a separate API:
API_SERVER=https://api.example.com
NEXT_PUBLIC_API_SERVER=https://api.example.com
